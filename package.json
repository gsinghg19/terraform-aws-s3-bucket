{
    "name": "terraform-aws-s3-bucket",
    "private": true,
    "version": "1.0.0",
    "description": "TypeScript Lambda utilities for terraform-aws-s3-bucket",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "watch": "tsc -w -p tsconfig.json",
        "clean": "rm -rf dist",
        "typecheck": "tsc --noEmit -p tsconfig.json"
    },
    "devDependencies": {
        "@babel/core": "^7.29.0",
        "@babel/preset-env": "^7.29.0",
        "@jest/globals": "^30.2.0",
        "@types/aws-lambda": "^8.10.161",
        "@types/jest": "^30.0.0",
        "@types/node": "^22.19.13",
        "babel-jest": "^30.2.0",
        "esbuild": "^0.27.3",
        "jest": "^30.2.0",
        "typescript": "^5.9.3"
    },
    "dependencies": {
        "@aws-sdk/client-s3": "^3.1001.0",
        "sharp": "^0.34.5"
    }
}
